import 'package:flutter/material.dart';

class MyMusicScreen extends StatelessWidget {
  const MyMusicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('myMusic', style: TextStyle(color: Colors.white)),
    );
  }
}
